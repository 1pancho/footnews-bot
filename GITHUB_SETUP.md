# 🐙 Инструкция по загрузке на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте [GitHub](https://github.com)
2. Войдите в свой аккаунт
3. Нажмите на **+** в правом верхнем углу → **New repository**
4. Заполните информацию:
   - **Repository name**: `news-parser-bot` (или любое другое название)
   - **Description**: `⚽️ Telegram bot для парсинга футбольных новостей`
   - **Visibility**: Public или Private (на ваш выбор)
   - ⚠️ **НЕ** ставьте галочки "Add README" или "Add .gitignore" (у нас уже есть эти файлы)
5. Нажмите **Create repository**

## Шаг 2: Свяжите локальный репозиторий с GitHub

После создания репозитория на GitHub, выполните команды:

```bash
# Замените username на ваш GitHub username
git remote add origin https://github.com/username/news-parser-bot.git

# Или используйте SSH (если настроен)
git remote add origin git@github.com:username/news-parser-bot.git
```

## Шаг 3: Отправьте код на GitHub

```bash
# Отправьте код
git push -u origin main

# Если возникла ошибка с веткой, попробуйте:
git branch -M main
git push -u origin main
```

## Шаг 4: Проверьте загрузку

1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта
3. README.md будет отображаться на главной странице

## 🔒 Важно: Безопасность

⚠️ **НИКОГДА не коммитьте файл .env с токенами!**

Файл `.env` уже добавлен в [.gitignore](.gitignore), но на всякий случай проверьте:

```bash
# Проверка, что .env не попал в git
git status

# Если .env случайно добавлен, удалите его:
git rm --cached .env
git commit -m "Remove .env from tracking"
git push
```

## 📝 Настройка GitHub Secrets (для CI/CD)

Если планируете использовать GitHub Actions для автоматического деплоя:

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте секреты:
   - Name: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен бота
5. Сохраните

## 🚀 Клонирование на другой компьютер

Чтобы скачать проект на другой компьютер:

```bash
# Клонировать репозиторий
git clone https://github.com/username/news-parser-bot.git
cd news-parser-bot

# Создать .env файл
cp .env.example .env
nano .env  # Добавить TELEGRAM_BOT_TOKEN

# Установить зависимости и запустить
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```

## 🔄 Обновление кода

При внесении изменений:

```bash
# Проверить изменения
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

## 📋 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения
git diff

# Отменить изменения в файле
git checkout -- filename

# Создать новую ветку
git checkout -b feature-name

# Переключиться на другую ветку
git checkout main

# Объединить ветки
git merge feature-name
```

## 🎨 Красивый README на GitHub

Ваш [README.md](README.md) уже содержит:
- ✅ Эмодзи для визуальной привлекательности
- ✅ Структурированные разделы
- ✅ Примеры кода с подсветкой синтаксиса
- ✅ Ссылки на дополнительную документацию

GitHub автоматически отобразит его красиво!

## 🏷️ Добавление тегов (releases)

Когда захотите создать версию:

```bash
# Создать тег
git tag -a v1.0.0 -m "Release version 1.0.0"

# Отправить тег на GitHub
git push origin v1.0.0

# Или отправить все теги
git push --tags
```

Затем на GitHub:
1. Перейдите в **Releases**
2. Нажмите **Create a new release**
3. Выберите тег и добавьте описание

## 🤝 Collaboration

Если хотите принимать вклад от других разработчиков:

1. Убедитесь, что репозиторий Public
2. Добавьте **CONTRIBUTING.md** с правилами
3. Настройте **Issues** и **Pull Requests**
4. Добавьте **LICENSE** файл (MIT рекомендуется)

## 📊 GitHub Actions

В проекте уже настроены GitHub Actions:
- [.github/workflows/docker-build.yml](.github/workflows/docker-build.yml) - сборка Docker образа
- [.github/workflows/python-lint.yml](.github/workflows/python-lint.yml) - проверка кода

Они запустятся автоматически при пуше кода!

---

Готово! Ваш проект на GitHub 🎉
