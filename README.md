# ⚽️ Футбольный новостной бот

Telegram-бот для парсинга и отслеживания футбольных новостей из крупных российских изданий.

## 📰 Возможности

- Парсинг новостей из нескольких источников:
  - Sports.ru
  - Championat.com
  - Soccer.ru
- Выбор интересующих футбольных клубов
- Фильтрация новостей по выбранным клубам
- Автоматическое обновление новостей каждые 30 минут
- Удобный интерфейс с кнопками и командами

## 🚀 Быстрый старт

### Вариант 1: Локальный запуск

#### 1. Клонируйте репозиторий

```bash
git clone https://github.com/1pancho/footnews-bot.git
cd news-parser-bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
venv\Scripts\activate  # для Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Создайте Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота

### 5. Настройте конфигурацию

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и укажите токен вашего бота:

```
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

## 🎮 Запуск

Запустите бота:

```bash
python main.py
```

### Вариант 2: Запуск через Docker

```bash
# 1. Создайте .env файл
cp .env.example .env
nano .env  # Добавьте TELEGRAM_BOT_TOKEN

# 2. Запустите контейнер
docker-compose up -d

# 3. Просмотр логов
docker-compose logs -f
```

### Вариант 3: Деплой на VPS

Автоматическая установка на ваш сервер:

```bash
./deploy/install_on_server.sh
```

Подробная инструкция: [DEPLOY.md](DEPLOY.md)

## 📱 Использование

### Команды бота:

- `/start` - Начать работу с ботом
- `/clubs` - Управление клубами
- `/news` - Получить последние новости
- `/help` - Показать справку

### Кнопки в меню:

- **📰 Новости** - Получить последние новости по вашим клубам
- **⚽️ Мои клубы** - Управление списком клубов
- **➕ Добавить клуб** - Добавить клуб в избранное
- **❌ Удалить клуб** - Удалить клуб из избранного
- **ℹ️ Помощь** - Показать справку

## ⚙️ Настройки

В файле `.env` можно настроить:

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `DATABASE_URL` - путь к базе данных SQLite
- `PARSE_INTERVAL_MINUTES` - интервал обновления новостей (в минутах)
- `MAX_NEWS_PER_REQUEST` - максимальное количество новостей за один запрос

## 📁 Структура проекта

```
news-parser-bot/
├── bot/                  # Telegram бот
│   ├── handlers.py      # Обработчики команд и callback
│   ├── keyboards.py     # Клавиатуры для бота
│   └── messages.py      # Текстовые сообщения
├── database/            # База данных
│   ├── models.py        # Модели базы данных
│   └── database.py      # Работа с БД
├── parsers/             # Парсеры новостей
│   ├── base_parser.py   # Базовый класс парсера
│   ├── sports_ru.py     # Парсер Sports.ru
│   ├── championat.py    # Парсер Championat.com
│   └── soccer_ru.py     # Парсер Soccer.ru
├── deploy/              # Скрипты для деплоя
│   ├── install_on_server.sh   # Установка на VPS
│   ├── start_bot.sh           # Запуск бота
│   ├── update_bot.sh          # Обновление бота
│   ├── setup_systemd.sh       # Настройка автозапуска
│   └── quick_deploy.sh        # Быстрый деплой
├── .github/workflows/   # GitHub Actions CI/CD
├── config.py            # Конфигурация
├── news_service.py      # Сервис для работы с новостями
├── main.py              # Точка входа
├── Dockerfile           # Docker образ
├── docker-compose.yml   # Docker Compose конфигурация
├── requirements.txt     # Зависимости
├── README.md            # Документация
└── DEPLOY.md            # Инструкция по деплою
```

## 🔧 Технологии

- **Python 3.11** - язык программирования
- **aiogram 3.4** - асинхронный фреймворк для Telegram ботов
- **SQLAlchemy** - ORM для работы с базой данных
- **BeautifulSoup4** - парсинг HTML
- **APScheduler** - планировщик задач
- **aiohttp** - асинхронные HTTP-запросы
- **Docker** - контейнеризация
- **GitHub Actions** - CI/CD

## 🌍 Платформы для деплоя

Бот готов к деплою на:
- ✅ **Свой VPS** (автоматические скрипты)
- ✅ **Railway.app** (бесплатный tier)
- ✅ **Render.com** (бесплатный tier)
- ✅ **Fly.io** (бесплатный tier)
- ✅ **Любой сервер с Docker**

Подробная инструкция: **[DEPLOY.md](DEPLOY.md)**

## 📝 Примечания

- Парсеры используют CSS-селекторы, которые могут измениться при обновлении сайтов. В этом случае нужно будет обновить селекторы в файлах парсеров.
- База данных создается автоматически при первом запуске.
- Логи сохраняются в файл `bot.log`.

## 🐛 Возможные проблемы

### Бот не отвечает
- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен
- Проверьте логи в `bot.log`

### Не парсятся новости
- Проверьте доступность новостных сайтов
- Возможно изменилась структура HTML - нужно обновить селекторы в парсерах
- Проверьте логи на наличие ошибок

### Ошибки базы данных
- Удалите файл `news_bot.db` и перезапустите бота
- Проверьте права доступа к файлу базы данных

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Создано с помощью Claude Code

---

**Приятного использования! ⚽️📰**
