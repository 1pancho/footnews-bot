# 🔄 Настройка автоматического деплоя через GitHub Actions

Этот гайд поможет настроить автоматический деплой бота на VPS при каждом push в `main` ветку.

---

## 📋 Требования

- GitHub репозиторий с кодом бота
- VPS сервер с SSH доступом
- Docker установлен на сервере

---

## 🔐 Шаг 1: Настройка GitHub Secrets

### 1.1. Откройте настройки репозитория

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите **Settings** (Настройки)
3. Слева выберите **Secrets and variables** → **Actions**

### 1.2. Добавьте следующие secrets:

Нажмите **New repository secret** для каждого:

#### `VPS_HOST`
- **Name**: `VPS_HOST`
- **Value**: `212.113.106.241` (IP адрес вашего сервера)

#### `VPS_USERNAME`
- **Name**: `VPS_USERNAME`
- **Value**: `root` (или ваш пользователь)

#### `VPS_PASSWORD`
- **Name**: `VPS_PASSWORD`
- **Value**: ваш пароль от сервера

⚠️ **Рекомендация**: Лучше использовать SSH-ключ вместо пароля (см. ниже)

#### `TELEGRAM_BOT_TOKEN`
- **Name**: `TELEGRAM_BOT_TOKEN`
- **Value**: ваш токен от @BotFather

---

## 🔑 Шаг 2: Использование SSH-ключа (Рекомендуется)

Вместо пароля безопаснее использовать SSH-ключ.

### 2.1. Создайте SSH-ключ

```bash
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions_key -N ""
```

### 2.2. Скопируйте ключ на сервер

```bash
ssh-copy-id -i ~/.ssh/github_actions_key.pub root@212.113.106.241
```

### 2.3. Получите приватный ключ

```bash
cat ~/.ssh/github_actions_key
```

Скопируйте весь вывод (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

### 2.4. Добавьте в GitHub Secrets

- **Name**: `VPS_SSH_KEY`
- **Value**: содержимое приватного ключа

### 2.5. Обновите workflow файл

Измените [.github/workflows/deploy.yml](.github/workflows/deploy.yml):

```yaml
- name: Deploy to VPS
  uses: appleboy/ssh-action@v1.0.3
  with:
    host: ${{ secrets.VPS_HOST }}
    username: ${{ secrets.VPS_USERNAME }}
    key: ${{ secrets.VPS_SSH_KEY }}  # Используйте ключ вместо пароля
    port: 22
    script: |
      # ... остальной скрипт
```

---

## 🚀 Шаг 3: Первоначальная настройка сервера

Перед первым автоматическим деплоем выполните на сервере:

```bash
ssh root@212.113.106.241

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
apt-get install -y docker-compose-plugin git

# Запустите Docker
systemctl start docker
systemctl enable docker

# Клонируйте репозиторий
cd /root
git clone https://github.com/1pancho/footnews-bot.git football-news-bot

# Всё готово!
```

---

## ✅ Шаг 4: Проверка работы

### 4.1. Сделайте любое изменение

```bash
# Измените что-нибудь в коде
echo "# Test" >> README.md

# Закоммитьте и запушьте
git add README.md
git commit -m "Test automatic deployment"
git push
```

### 4.2. Проверьте GitHub Actions

1. Перейдите в **Actions** в вашем репозитории
2. Вы должны увидеть новый workflow "Deploy to VPS"
3. Нажмите на него, чтобы увидеть логи

### 4.3. Проверьте бота

1. Откройте Telegram
2. Отправьте боту `/start`
3. Бот должен ответить!

---

## 🔧 Ручной запуск деплоя

Вы можете запустить деплой вручную:

1. Перейдите в **Actions**
2. Выберите **Deploy to VPS**
3. Нажмите **Run workflow**
4. Выберите ветку `main`
5. Нажмите **Run workflow**

---

## 📊 Что происходит при деплое

1. **GitHub Actions** запускается при push в `main`
2. Подключается к вашему VPS через SSH
3. Переходит в директорию проекта
4. Делает `git pull` для получения последних изменений
5. Создаёт `.env` файл (если его нет) с токеном из secrets
6. Останавливает старый контейнер
7. Собирает и запускает новый контейнер
8. Показывает статус и логи

---

## 🐛 Устранение неполадок

### Ошибка подключения SSH

```
Permission denied (publickey,password)
```

**Решение:**
- Проверьте правильность `VPS_HOST`, `VPS_USERNAME`, `VPS_PASSWORD`
- Если используете SSH-ключ, проверьте что он правильно добавлен

### Ошибка "directory not found"

```
cd: /root/football-news-bot: No such file or directory
```

**Решение:**
Клонируйте репозиторий на сервер вручную:
```bash
ssh root@212.113.106.241
cd /root
git clone https://github.com/1pancho/footnews-bot.git football-news-bot
```

### Docker ошибки

**Решение:**
Проверьте что Docker установлен и запущен:
```bash
ssh root@212.113.106.241
systemctl status docker
docker --version
```

---

## 🔒 Безопасность

### ✅ Рекомендации:

1. **Используйте SSH-ключи** вместо паролей
2. **Не делайте репозиторий публичным** если там есть чувствительные данные
3. **Регулярно ротируйте токены** и пароли
4. **Мониторьте логи деплоя** на наличие ошибок
5. **Используйте отдельного пользователя** для деплоя (не root)

### ❌ Не делайте:

1. Не храните секреты в коде
2. Не коммитьте `.env` файлы
3. Не делайте репозиторий публичным с secrets в Actions

---

## 📝 Проверочный список

- [ ] GitHub Secrets настроены (VPS_HOST, VPS_USERNAME, VPS_PASSWORD, TELEGRAM_BOT_TOKEN)
- [ ] Docker установлен на сервере
- [ ] Репозиторий склонирован на сервер в `/root/football-news-bot`
- [ ] Workflow файл `.github/workflows/deploy.yml` существует
- [ ] Первый деплой запущен успешно
- [ ] Бот отвечает в Telegram

---

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [SSH Action Documentation](https://github.com/appleboy/ssh-action)
- [Docker Compose Documentation](https://docs.docker.com/compose/)

---

## 🎯 Следующие шаги

После настройки автоматического деплоя:

1. **Добавьте тесты** - создайте workflow для запуска тестов перед деплоем
2. **Добавьте уведомления** - настройте уведомления в Telegram о статусе деплоя
3. **Staging окружение** - создайте отдельную ветку `develop` для тестирования
4. **Мониторинг** - настройте мониторинг работы бота

---

**Теперь при каждом push в `main` бот будет автоматически обновляться на сервере!** 🎉
