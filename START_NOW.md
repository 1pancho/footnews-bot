# 🚀 Запуск бота - Пошаговая инструкция

## ✅ Что нужно сделать прямо сейчас:

### Шаг 1: Проверьте GitHub Secrets (2 минуты)

Откройте: **https://github.com/1pancho/footnews-bot/settings/secrets/actions**

Должны быть 4 секрета:

| Название | Что ввести |
|----------|-----------|
| `VPS_HOST` | `212.113.106.241` |
| `VPS_USERNAME` | `root` |
| `VPS_PASSWORD` | ваш **новый** пароль сервера |
| `TELEGRAM_BOT_TOKEN` | токен от @BotFather |

❓ **Нет токена?** → Идите в Telegram:
1. Найдите @BotFather
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

### Шаг 2: Запустите деплой вручную (1 минута)

1. Откройте: **https://github.com/1pancho/footnews-bot/actions**
2. Слева выберите **"Manual Deploy"** или **"Deploy to VPS"**
3. Справа нажмите **"Run workflow"**
4. Нажмите зелёную кнопку **"Run workflow"**
5. Подождите 2-3 минуты

---

### Шаг 3: Проверьте результат

#### ✅ Если увидите зелёную галочку:
- Бот работает!
- Откройте Telegram → найдите бота → отправьте `/start`

#### ❌ Если красный крестик:
- Нажмите на failed workflow
- Скопируйте ошибку
- Напишите мне текст ошибки

---

## 🆘 Альтернативный способ (если GitHub Actions не работает)

### Запуск напрямую через SSH:

```bash
# 1. Подключитесь к серверу
ssh root@212.113.106.241

# 2. Выполните эти команды
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
apt-get install -y docker-compose-plugin git
systemctl start docker

# 3. Клонируйте проект
cd /root
git clone https://github.com/1pancho/footnews-bot.git football-news-bot
cd football-news-bot

# 4. Создайте .env файл
nano .env
```

В nano вставьте (замените `ВАШ_ТОКЕН`):

```env
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
DATABASE_URL=sqlite+aiosqlite:///./data/news_bot.db
PARSE_INTERVAL_MINUTES=30
MAX_NEWS_PER_REQUEST=10
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

```bash
# 5. Запустите бота
mkdir -p data logs
docker compose up -d --build

# 6. Проверьте логи
docker compose logs -f
```

---

## 📋 Чеклист:

- [ ] GitHub Secrets добавлены (все 4)
- [ ] Токен бота получен от @BotFather
- [ ] GitHub Actions запущен
- [ ] Workflow завершился успешно (зелёная галочка)
- [ ] Бот отвечает в Telegram на `/start`

---

## ❓ Частые проблемы:

### "Permission denied"
→ Проверьте `VPS_PASSWORD` в secrets - это **новый** пароль?

### "Bot token is invalid"
→ Получите новый токен у @BotFather:
- `/mybots` → выберите бота → `API Token` → `Revoke` → скопируйте новый

### "Docker not found"
→ Используйте альтернативный способ выше (запуск через SSH)

---

## 💬 Нужна помощь?

Напишите мне:
1. Что именно не работает?
2. Какая ошибка в GitHub Actions? (скриншот или текст)
3. Что показывает `docker compose logs` на сервере?

---

**Начните с Шага 1 → Шага 2 → Шага 3. Это займет 3-5 минут!** ⏱️
