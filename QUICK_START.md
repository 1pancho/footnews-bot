# ⚡️ Быстрый старт для вашего VPS

Упрощенная инструкция для деплоя на ваш сервер **212.113.106.241**

## 🚀 Вариант 1: Автоматическая установка (Рекомендуется)

### С вашего компьютера:

```bash
# 1. Перейдите в директорию проекта
cd /Users/medov/news-parser-bot

# 2. Запустите автоматическую установку
./deploy/install_on_server.sh
# Введите пароль: 8ce6TqVhw52C

# 3. Дождитесь завершения установки
```

### На сервере:

```bash
# 1. Подключитесь к серверу
ssh root@212.113.106.241

# 2. Перейдите в директорию
cd /root/football-news-bot

# 3. Создайте токен бота в @BotFather
# Откройте Telegram → @BotFather → /newbot

# 4. Добавьте токен в .env
nano .env
# Вставьте: TELEGRAM_BOT_TOKEN=ваш_токен_здесь
# Сохраните: Ctrl+O, Enter, Ctrl+X

# 5. Запустите бота
./deploy/start_bot.sh

# 6. Проверьте логи
docker-compose logs -f
```

## 🔄 Вариант 2: Быстрое обновление

Если бот уже установлен и нужно только обновить код:

```bash
# С вашего компьютера
./deploy/quick_deploy.sh
```

## 🤖 Настройка автозапуска

Чтобы бот запускался автоматически при перезагрузке сервера:

```bash
# На сервере
cd /root/football-news-bot
sudo ./deploy/setup_systemd.sh
```

## 📊 Полезные команды

### На сервере:

```bash
# Просмотр логов
docker-compose logs -f

# Остановка бота
docker-compose down

# Запуск бота
docker-compose up -d

# Перезапуск бота
docker-compose restart

# Обновление бота
./deploy/update_bot.sh

# Статус контейнера
docker-compose ps
```

### С вашего компьютера:

```bash
# Подключение к серверу
ssh root@212.113.106.241

# Просмотр логов удаленно
ssh root@212.113.106.241 'cd /root/football-news-bot && docker-compose logs --tail=50'

# Быстрый деплой
./deploy/quick_deploy.sh
```

## 🔒 Настройка SSH-ключа (Рекомендуется)

Чтобы не вводить пароль каждый раз:

```bash
# 1. Создайте SSH ключ (если нет)
ssh-keygen -t ed25519

# 2. Скопируйте ключ на сервер
ssh-copy-id root@212.113.106.241
# Введите пароль: 8ce6TqVhw52C

# 3. Теперь можно подключаться без пароля
ssh root@212.113.106.241
```

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте токен
cat .env

# Проверьте логи
docker-compose logs

# Пересоберите контейнер
docker-compose down
docker-compose up -d --build
```

### Не удается подключиться к серверу

```bash
# Проверьте доступность
ping 212.113.106.241

# Попробуйте подключиться с verbose
ssh -v root@212.113.106.241
```

### Docker ошибки

```bash
# Проверьте статус Docker
systemctl status docker

# Запустите Docker
systemctl start docker

# Проверьте права
ls -la /var/run/docker.sock
```

## 📱 Проверка работы бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот отвечает - всё работает! ✅

## 🎯 Что дальше?

1. **Настройте автообновление новостей** - проверьте `PARSE_INTERVAL_MINUTES` в `.env`
2. **Добавьте больше клубов** - отредактируйте `FOOTBALL_CLUBS` в [config.py](config.py)
3. **Мониторинг** - настройте уведомления об ошибках
4. **Резервное копирование** - периодически копируйте `/root/football-news-bot/data/news_bot.db`

## 📚 Дополнительная документация

- [README.md](README.md) - основная документация
- [DEPLOY.md](DEPLOY.md) - подробная инструкция по деплою
- [GITHUB_SETUP.md](GITHUB_SETUP.md) - загрузка на GitHub

---

**Готово! Бот должен работать на вашем сервере** 🎉

Если возникли вопросы - проверьте логи: `docker-compose logs -f`
