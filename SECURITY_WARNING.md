# ⚠️ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ БЕЗОПАСНОСТИ

## 🔴 Скомпрометированные данные

В результате ошибки следующая чувствительная информация была временно выложена в публичный репозиторий:

1. **Пароль от сервера**
2. **Токен Telegram бота**

## ✅ Немедленные действия

### 1. Смените пароль сервера

```bash
# Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# Смените пароль
passwd

# Введите новый надежный пароль
```

### 2. Создайте нового бота (рекомендуется)

**Вариант A: Отзовите токен старого бота**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите `API Token` → `Revoke current token`

**Вариант B: Создайте нового бота**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Создайте нового бота с другим именем
4. Используйте новый токен

### 3. Обновите .env на сервере

```bash
ssh root@YOUR_SERVER_IP
cd /root/football-news-bot
nano .env

# Замените TELEGRAM_BOT_TOKEN на новый токен
# Сохраните: Ctrl+O, Enter, Ctrl+X

# Перезапустите бота
docker compose down
docker compose up -d
```

---

## 🛡️ Дальнейшие меры безопасности

### 1. Используйте SSH-ключи

```bash
# Сгенерируйте ключ
ssh-keygen -t ed25519

# Скопируйте на сервер
ssh-copy-id root@YOUR_SERVER_IP

# На сервере отключите вход по паролю
sudo nano /etc/ssh/sshd_config
# Измените: PasswordAuthentication no
sudo systemctl restart sshd
```

### 2. Настройте firewall

```bash
ssh root@YOUR_SERVER_IP

# Настройка UFW
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

### 3. Мониторинг входов

```bash
# Просмотр последних входов
last

# Просмотр попыток входа
grep "Failed password" /var/log/auth.log
```

---

## 📋 Чеклист безопасности

- [ ] Пароль сервера изменен
- [ ] Токен бота отозван или создан новый бот
- [ ] .env файл обновлен на сервере
- [ ] Бот перезапущен с новым токеном
- [ ] SSH-ключи настроены
- [ ] Вход по паролю отключен
- [ ] Firewall настроен
- [ ] Проверен список последних входов

---

## 🔐 Правила безопасности

### ✅ ДЕЛАЙТЕ:

- Используйте переменные окружения для секретов
- Храните .env локально, не коммитьте в git
- Используйте SSH-ключи вместо паролей
- Регулярно меняйте пароли
- Используйте сильные пароли (20+ символов)
- Включите двухфакторную аутентификацию где возможно

### ❌ НЕ ДЕЛАЙТЕ:

- Не храните пароли в коде
- Не коммитьте .env файлы
- Не публикуйте токены в документации
- Не используйте простые пароли
- Не используйте одинаковые пароли для разных сервисов

---

## 📞 Если нужна помощь

1. **GitHub Issues**: https://github.com/1pancho/footnews-bot/issues
2. **Документация**: [DEPLOY_INSTRUCTIONS.md](DEPLOY_INSTRUCTIONS.md)

---

**История была очищена, но для полной безопасности смените учетные данные!**
