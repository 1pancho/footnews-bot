# 🚀 Инструкция по деплою

Полное руководство по развертыванию Football News Bot на различных платформах.

## 📋 Содержание

- [Деплой на свой VPS](#деплой-на-свой-vps)
- [Деплой через Docker](#деплой-через-docker)
- [Деплой на Railway.app](#деплой-на-railway)
- [Деплой на Render.com](#деплой-на-render)
- [Деплой на Fly.io](#деплой-на-flyio)

---

## 🖥 Деплой на свой VPS

### Автоматическая установка (Рекомендуется)

**Шаг 1: Установка на сервер**

```bash
# Запустите с вашего компьютера
./deploy/install_on_server.sh
```

Скрипт автоматически:
- Установит все необходимые пакеты
- Установит Docker и Docker Compose
- Скопирует файлы проекта на сервер

**Шаг 2: Настройка .env файла**

```bash
# Подключитесь к серверу
ssh root@212.113.106.241

# Перейдите в директорию проекта
cd /root/football-news-bot

# Отредактируйте .env файл
nano .env
```

Добавьте токен бота:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

**Шаг 3: Запуск бота**

```bash
# Запуск через Docker Compose
./deploy/start_bot.sh
```

**Шаг 4: Настройка автозапуска (опционально)**

```bash
# Настройка systemd service для автозапуска при перезагрузке сервера
sudo ./deploy/setup_systemd.sh
```

### Быстрое обновление

Для быстрого обновления бота на сервере:

```bash
# Запустите с вашего компьютера
./deploy/quick_deploy.sh
```

Или вручную на сервере:

```bash
ssh root@212.113.106.241
cd /root/football-news-bot
./deploy/update_bot.sh
```

### Управление ботом на сервере

```bash
# Просмотр логов
docker-compose logs -f

# Остановка бота
docker-compose down

# Запуск бота
docker-compose up -d

# Перезапуск бота
docker-compose restart

# Статус контейнера
docker-compose ps

# Просмотр использования ресурсов
docker stats
```

Если настроен systemd service:

```bash
# Статус сервиса
systemctl status football-news-bot

# Запуск
systemctl start football-news-bot

# Остановка
systemctl stop football-news-bot

# Перезапуск
systemctl restart football-news-bot

# Логи
journalctl -u football-news-bot -f
```

---

## 🐳 Деплой через Docker (на любом сервере)

### Предварительные требования

- Docker 20.10+
- Docker Compose 2.0+

### Установка Docker (если не установлен)

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
sudo apt-get install docker-compose-plugin

# Проверка установки
docker --version
docker compose version
```

### Запуск

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/yourusername/news-parser-bot.git
cd news-parser-bot

# 2. Создайте .env файл
cp .env.example .env
nano .env  # Добавьте TELEGRAM_BOT_TOKEN

# 3. Запустите контейнер
docker-compose up -d

# 4. Проверьте логи
docker-compose logs -f
```

---

## 🚂 Деплой на Railway.app

Railway - бесплатная платформа для хостинга с $5 кредитами в месяц.

### Через веб-интерфейс

1. Зайдите на [Railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **New Project** → **Deploy from GitHub repo**
4. Выберите ваш репозиторий
5. Railway автоматически обнаружит `railway.json`
6. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` - ваш токен бота
7. Деплой запустится автоматически

### Через CLI

```bash
# Установка Railway CLI
npm install -g @railway/cli

# Вход в аккаунт
railway login

# Инициализация проекта
railway init

# Добавление переменных
railway variables set TELEGRAM_BOT_TOKEN=ваш_токен

# Деплой
railway up
```

---

## 🎨 Деплой на Render.com

Render предоставляет бесплатный tier для worker-сервисов.

### Через веб-интерфейс

1. Зайдите на [Render.com](https://render.com)
2. Нажмите **New** → **Blueprint**
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml`
5. Добавьте переменную окружения:
   - `TELEGRAM_BOT_TOKEN` - ваш токен бота
6. Нажмите **Apply**

### Вручную

1. **New** → **Background Worker**
2. Подключите репозиторий
3. Настройки:
   - **Environment**: Docker
   - **Dockerfile Path**: `./Dockerfile`
4. Добавьте переменные окружения
5. Нажмите **Create Background Worker**

---

## ✈️ Деплой на Fly.io

Fly.io - платформа с бесплатным tier (3 VM бесплатно).

### Установка Fly CLI

```bash
# macOS
brew install flyctl

# Linux
curl -L https://fly.io/install.sh | sh

# Windows
powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
```

### Деплой

```bash
# 1. Вход в аккаунт
flyctl auth login

# 2. Создание приложения (fly.toml уже настроен)
flyctl apps create football-news-bot

# 3. Создание volume для базы данных
flyctl volumes create bot_data --size 1

# 4. Установка токена бота
flyctl secrets set TELEGRAM_BOT_TOKEN=ваш_токен

# 5. Деплой
flyctl deploy

# 6. Проверка статуса
flyctl status

# 7. Просмотр логов
flyctl logs
```

---

## 🔧 Настройка переменных окружения

Обязательные переменные:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

Опциональные переменные:

```bash
DATABASE_URL=sqlite+aiosqlite:///./news_bot.db
PARSE_INTERVAL_MINUTES=30
MAX_NEWS_PER_REQUEST=10
```

---

## 📊 Мониторинг и логи

### Docker Compose

```bash
# Просмотр логов
docker-compose logs -f

# Статистика использования
docker stats

# Проверка здоровья контейнера
docker-compose ps
```

### Проверка работы бота

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Если бот отвечает - всё работает!

---

## 🐛 Устранение неполадок

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs

# Проверьте переменные окружения
cat .env

# Пересоберите контейнер
docker-compose down
docker-compose up -d --build
```

### Бот не отвечает

1. Проверьте токен в `.env`
2. Убедитесь, что контейнер запущен: `docker-compose ps`
3. Проверьте логи на ошибки: `docker-compose logs`

### Проблемы с парсингом

Если новости не парсятся:
1. Проверьте доступность сайтов из контейнера
2. Возможно изменилась структура HTML - обновите селекторы в `parsers/*.py`

---

## 🔒 Безопасность

### Важные рекомендации:

1. **Никогда не коммитьте .env файл в git**
2. **Используйте secrets для токенов** на CI/CD платформах
3. **Регулярно обновляйте зависимости**: `pip install --upgrade -r requirements.txt`
4. **Настройте firewall** на VPS
5. **Используйте SSH-ключи** вместо паролей

### Настройка SSH-ключа (рекомендуется)

```bash
# Генерация ключа (если нет)
ssh-keygen -t ed25519

# Копирование ключа на сервер
ssh-copy-id root@212.113.106.241

# Теперь можно подключаться без пароля
ssh root@212.113.106.241
```

---

## 📝 Полезные ссылки

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Railway Documentation](https://docs.railway.app/)
- [Render Documentation](https://render.com/docs)
- [Fly.io Documentation](https://fly.io/docs/)

---

## 💡 Советы по оптимизации

1. **Уменьшите интервал парсинга** для экономии ресурсов (в `.env`)
2. **Настройте логротацию** для предотвращения переполнения диска
3. **Мониторьте использование памяти** с помощью `docker stats`
4. **Регулярно чистите старые новости** из БД (можно добавить в код)

---

Если возникли вопросы или проблемы - создайте Issue в GitHub репозитории!
